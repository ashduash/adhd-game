# 专注风暴 · 产品与市场优化建议书

> 视角：产品经理 + 市场经理
> 对象：微信小游戏「专注风暴」（Canvas 全自绘、8 种注意力训练模式）
> 方式：通读全部核心代码（入口 / 13 个场景 / 基类 / 配置 / 工具 / 后端约定）后，从玩家体验、留存、变现、增长、UI 五个维度给出的可落地建议。
> 结论先行：底层架构扎实、玩法覆盖专业，但**存在 2 个 P0 级阻断性缺陷（每日/训练计划网格崩坏、9 个成就永久不可解锁）和 1 个 P1 级变现链路断裂（广告占位未填）**，需在正式推广前修复；同时**完全缺失微信生态的社交裂变能力**，是增长端最大的机会缺口。

---

## 一、产品体验总览

**它是什么**：一款主打"注意力/专注力训练"的微信小游戏，8 种模式覆盖注意力、工作记忆、视觉搜索、抑制控制、反应速度、模式识别、序列加工、注意力分配；配套段位、成就、每日挑战、5 天训练计划、皮肤系统。

**做得好的地方（资产，应保留并放大）**：
- 场景栈架构清晰，Canvas 全自绘，与微信原生 API 解耦良好，跨机型适配（DPR/高刷/安全区）处理到位。
- 8 种玩法对应不同认知维度，专业感强，区别于市面上纯"反应点击"类小游戏。
- 段位 + 成就 + 每日 + 训练计划 + 皮肤，留存钩子体系完整。
- 内容安全三重防线（本地词库 → 云函数 → 服务端）合规到位。
- 暂停/恢复、首次引导、规则弹窗、退出确认等细节完善。

**核心问题（一句话）**：地基稳、内容多，但**关键链路有 bug、奖励闭环断裂、且完全没有社交裂变**——这三点会直接卡住"能不能玩、愿不愿意留、能不能涨"。

---

## 二、必修 Bug（按严重度排序）

### 🔴 P0-1｜每日挑战 / 训练计划引用了"不存在的难度档"，导致网格渲染崩坏

**现象**：扫视（scan）与排序（sort）的"每日挑战"和"训练计划"会生成正方形网格不支持的难度档，进入后棋盘错位、单元格重叠、点击命中错误，基本无法游玩。

**根因**：各模式实际可选难度都是**完全平方数网格**，但配置里写的是非平方数：

| 模式 | 实际难度档（场景定义） | 每日/训练里错误引用的档 |
|------|----------------------|------------------------|
| scan（闪电扫视） | `16 / 25 / 36 / 49 / 64`（scan-scene.js:145） | `15 / 20 / 25 / 30`（daily.js:24；training.js:11,30） |
| sort（序列排序） | `9 / 16 / 25 / 36`（sort-scene.js:124） | `6 / 8 / 10 / 12`（daily.js:28；training.js:20,41） |

渲染代码用 `cols = Math.sqrt(this.level)` 推算网格列数（scan-scene.js:165、sort-scene.js:146），一旦 `level` 不是完全平方数，`Math.sqrt` 得到小数 → 单元格坐标错位、重叠、溢出屏幕，且 `hitTestGrid` 命中区与视觉不一致。

**影响面**：
- 每日挑战：scan 在 15/20/30、sort 在所有档位（6/8/10/12）均崩坏。
- 训练计划："记忆力"天含 sort 8、"规划力"天含 sort 10 —— **5 天计划里有 2 天含坏关**，直接破坏核心留存功能。

**修复方向**：
1. 将 `daily.js` / `training.js` 中 scan/sort 的档位统一收敛到各自真实难度集（scan 用 16/25/36/49/64；sort 用 9/16/25/36）。
2. 更好的做法：抽一个 `MODE_CONFIG` 单一数据源，模式难度档**只定义一处**，每日/训练/准备页/评分全部引用它，从根上杜绝再次漂移。
3. 给 `initGame`/`_renderPlaying` 加防御：`level` 非完全平方数时回退到最近合法档并告警。

---

### 🔴 P0-2｜约 9 个成就"永远无法解锁"

**现象**：成就墙上有大量成就点亮条件，但游戏全程从未写入对应数据，导致这些成就**永久不可达**。

**证据**（grep 确认这些字段仅在 `achievements.js` 出现，无任何场景写入）：

| 成就 | 判定条件 | 实际写入情况 |
|------|---------|-------------|
| 连击达人（react_combo_10） | `react._maxCombo >= 10` | react 只存 `score = maxCombo` 数值，从不存在 `_maxCombo` |
| 不受干扰（stroop_perfect） | `stroop._perfect` | stroop 只存 `score = correct`，无 `_perfect` |
| 记忆新星 / 完美记忆 | `memory._perfect6 / _perfect10` | memory 只存 `score = correct`，无这两个字段 |
| 连锁大师 / 清盘高手 | `match._maxCombo / _cleared` | match 只存 `score` 数值，无这两个字段 |
| 分心大师 / 闪电反射 | `dual.expert==='S'` / `react.expert==='S'` | 这两个模式按难度存的是**数字分数**，`=== 'S'` 恒为 false |
| 挑战达人（daily_7） | `dailyChallenge.completedCount >= 7` | 全代码从未自增 `completedCount`（`_completeDailyAndTraining` 只设 `completed=true`） |

**影响**：25 个成就中约 9 个（36%）不可解锁，且其中多个是"技巧/速度"类高光成就，是最能驱动玩家炫耀与回访的部分。成就体系作为留存核心钩子被严重削弱。

**修复方向**：
1. 让各场景在 `_finishGameWithRating` 前，把成就需要的派生字段写进 `bestScores[mode]`（如 `_maxCombo`、`_perfect`、`_cleared`、各难度 `rating`）。
2. 或在 `achievements.js` 的 `check` 里改判现有已存字段（如 react 用 `bestScores.react[level] >= 10` 表示最高连击≥10 需另存；dual/react 的 S 评级应从存储的评级而非分数判断）。
3. 新增 `dailyChallenge.completedCount` 并在每日完成时 +1（同时顺手实现 README 承诺的打卡加成，见 P1-3）。
4. 建议加一个"成就自检脚本/单测"，防止再次定义不可达成就。

---

### 🟠 P1-1｜广告单元为占位符，所有"看视频"奖励链路失效

**现象**：用户点"看视频双倍积分 / 看视频领补签卡 / 看视频解锁皮肤"，会看到"广告加载中，请稍后"，然后什么都没发生——是一条**死胡同**。

**证据**：`ads.js:7-9` 的 `_AD_UNIT_IDS` 仍是 `adunit-xxx…` 占位符；`ads.js:20,69` 检测到含 `xxx`/`yyy` 时直接跳过创建 → `rewardedAd` / `interstitialAd` 为 `null`；`showRewardedAd` 因而永远 `resolve(false)`。

**双重影响**：
- **用户体验**：奖励闭环断裂，玩家付出"看广告"意图却无回报，信任受损。
- **商业变现**：未填真实广告单元 = 当前**零广告收入**，且无法验证激励视频/插屏的真实 eCPM 与填充。

**修复方向**：
1. 上线前在微信广告后台创建真实单元 ID 并替换占位符（这是发布硬门槛，README 也已标注）。
2. 在广告不可用时有**优雅降级**：例如"看视频"按钮改为置灰提示"暂未开放"，或临时用"分享得奖励"替代，避免死胡同。
3. 设计更克制的激励视频触发点（见"变现"章节），别让奖励依赖广告成为唯一路径。

---

### 🟠 P1-2｜强制登录门 + 社交功能封锁，首启高摩擦

**现象**：未登录时整个首页被全屏登录遮罩盖住，必须先"微信一键登录"才能玩任何东西；且未登录时排行榜/个人中心被禁止进入（scene-manager.js:69）。

**影响**：
- 把"想试玩"的用户挡在门外，首启流失率高。对休闲小游戏而言，应允许**游客即玩**，在需要社交/排行时再引导登录。
- 排行榜、个人中心（成就/数据展示的社交面）被锁，削弱炫耀与攀比动力。

**修复方向**：
1. 允许游客模式进入并游玩，本地记录成绩；首次产生"上榜/分享/补签"等社交意图时再弹登录。
2. 登录引导用轻量卡片而非全屏遮罩，且可一键跳过。
3. 明确登录收益（"登录后成绩计入全网排行"），用利益驱动而非强制。

---

### 🟠 P1-3｜README 承诺的"打卡/每日积分加成"未实现

**现象**：README（173–179 行）描述连续打卡 7/14/30 天分别 2/3/5 倍积分、每日挑战有"额外奖励"，但代码中**没有任何倍率/额外分逻辑**。`_finishGameWithRating`（scene.js:524-547）与 `_completeDailyAndTraining`（scene.js:75-91）只标记 `completed=true`。

**影响**：文档与体验不符；连续打卡的"实利感"缺失，用户看不到 2 倍的回报，打卡动力打折。

**修复方向**：在 `_finishGameWithRating` 中读取 `dailyChallenge.completed` 与 `streak`，对当局积分乘以对应系数并做 toast 提示（如"连续 7 天，积分 ×2！"）。同时补上 P0-2 的 `completedCount`，一举两得。

---

### 🟡 P2（体验/健壮性，建议近期修）

| 编号 | 问题 | 位置 | 建议 |
|------|------|------|------|
| P2-1 | scan 结算 `_gameFinished` 无 `gameState==='finished'` 守卫，计时归零与最后一击同帧存在理论双计 | scan-scene.js:76,103,109 | 加守卫（参考 match-scene.js:181），全场景统一 |
| P2-2 | 插屏广告在结算页即时弹出，覆盖结算动画/新纪录提示 | scene.js:542 | 改为延迟 1–2 秒，或返回首页时再弹，避免打断成就感 |
| P2-3 | 记忆游戏 gameState 为 `showing`/`input`（非 `playing`），返回键不弹退出确认，误触直接丢进度回首页 | scene.js:489、memory-scene.js | 与其它模式一致加二次确认；或记忆也纳入 `playing` 语义 |
| P2-4 | 评分逻辑分散：stroop/scan/sort 在场景内硬编码阈值，scoring.js 缺对应函数 | stroop:117、scan:116、sort:99 | 抽进 scoring.js，统一入口，避免与难度档漂移 |
| P2-5 | 首页统计只硬编码 schulte[4]/memory[6]，不能代表真实最佳 | home-scene.js:677-678 | 展示各模式"当前最佳/最高段位"或聚合指标 |
| P2-6 | 皮肤命名 README（warm/sakura）与代码（piano/neon）不一致 | README:198-207 vs skins.js | 统一文档与代码，避免误导 |
| P2-7 | react 假目标渲染为方形但命中按圆形，方形四角为"死区" | react-scene.js:154 vs :102 | 命中区与视觉统一（都按方形或都按圆形） |
| P2-8 | 清除数据后内存中 `unlockedSkins` 未重置，残留已解锁态 | profile-scene.js:161-168 | 清除时一并复位内存态 |

---

## 三、玩法创新建议（产品视角）

> 当前 8 种玩法已经很"满"，问题不在"不够多"，而在"玩家不知道为什么练、练完不知道有没有变好"。

1. **补上"价值叙事 / 科学背书"**：首页应有一句清晰的"为什么练"（如"每天 5 分钟，4 周提升专注力"），并用进度/报告让 ADHD/学生/职场用户感知收益。目前只有功能罗列，缺乏动机。
2. **训练报告 / 周报**：ADHD 与"想变专注"的用户最想要的是**"我有没有变好"**。建议每周生成一份可视化报告（各模式 S 率、反应速度趋势、连续天数），可分享。这是差异化的核心卖点。
3. **个性化推荐**：基于 `bestScores` 弱项，首页"今日推荐"自动推一个最该练的模式，而不是永远平铺 8 张卡片。
4. **限时活动 / 赛季**：用段位赛季、节日挑战、好友 PK 赛制造节奏感与紧迫感，避免"每天机械打卡"。
5. **难度自适应**：新手前几局自动降难度 + 更强的引导；高手开放更高档（目前 sort 最高 36、scan 最高 64 已够，但可加"无尽模式"）。
6. **成就体系升级**：修复 P0-2 后，新增"成长树 / 里程碑"式成就，让解锁有层次与惊喜，而非一堵静态墙。
7. **多人/对战**：双人同屏或异步"挑战好友"（见增长章节），把单机训练变成社交竞技。

---

## 四、UI / 交互重设计建议

1. **首次启动 Onboarding**：首次进入用 2–3 屏讲清"这是什么、怎么玩、练了有啥用"，而不是直接把登录遮罩 + 8 张卡砸脸上。
2. **"主题"与"皮肤"概念澄清**：现在有两套独立系统——首页/个人中心的深色/浅色**主题**，以及游戏内的 5 套**皮肤**（night/piano/neon/forest/ocean）。用户易混淆，且皮肤只能在单游戏准备页里换。建议统一为"外观"入口，首页即可预览与切换，并明确哪些免费、哪些看广告解锁（且广告要能真解锁，见 P1-1）。
3. **首页信息架构重排**：建议顺序——顶部段位/今日状态 → **继续游戏 / 今日推荐**（一键续玩）→ 每日挑战（带目标与奖励预览）→ 训练计划（今日 3 关进度）→ 8 模式网格。把"行动入口"前置，降低决策成本。
4. **结算页加入"分享"与"数据可视化"**：结算是情绪高点，应在此放"炫耀卡片 / 挑战好友"按钮（与增长联动），并用小图表展示本次 vs 历史最佳。
5. **深浅色一致性**：浅色主题（warm 纸调）下部分游戏内文字/描边对比度需复核，保证可读性；统一圆角、间距、阴影语言。
6. **原生弹窗 vs Canvas 弹窗风格割裂**：隐私政策/用户协议用 `wx.showModal`（系统样式），登录/退出/规则用 Canvas 自绘——两种视觉语言混用。建议统一为 Canvas 风格（或反之），保持品牌一致。
7. **空 / 加载态**：排行榜无数据时只有一行文字；加载用"加载中…"。建议加插画空状态与骨架屏，提升精致感。
8. **无障碍**：次要文字 `rgba(...,0.5)` 在亮屏下偏淡，关键数值建议提对比度；字号已偏大，保持。

---

## 五、市场与增长策略

### 5.1 最大缺口：微信生态社交裂变完全缺失
grep 确认代码中**没有任何分享/转发/邀请/挑战好友**能力（`onShareAppMessage`、分享按钮、群排行均无）。而微信小游戏的命脉就是**社交关系链分发**。

建议补齐（按 ROI 排序）：
1. **结算页"炫耀 + 挑战好友"**：生成带成绩的海报/卡片，一键转发；好友点开可直接"应战"（进入同模式同难度）。
2. **群排行榜**：基于云开发的群维度排行，刺激熟人圈比拼。
3. **每日/周"战报"可分享**：把 P3 的训练报告做成可转发卡片，天然带新。
4. **邀请有礼**：邀请好友完成首局，双方得皮肤/补签卡（用 P1-1 修好的广告或真实奖励）。

### 5.2 定位与卖点
- 当前定位"专注力训练"专业且清晰，但**获客词分散**。建议锁定 3 类人群做素材：
  - 学生/考研党："备考专注力"
  - 职场人："抗干扰、效率"
  - 家长给孩子："护眼脑力训练"（注意合规，不承诺医疗效果，现有用户协议已规避）
- 搜索词 / 商店文案围绕"舒尔特方格""专注力""脑力"布局。

### 5.3 留存与召回
- 利用**微信订阅消息**（原模板消息）做打卡提醒、训练计划到期提醒、好友挑战提醒——把"连续打卡"从被动变主动召回。
- 连续打卡断签的补签卡机制已具备，但需配合 P1-3 的实利加成才真正有吸引力。

### 5.4 变现（在修好 P1-1 基础上）
- **激励视频**：除现有"双倍积分/补签/皮肤"，可增加"复活一次""提示一次""重试不计次"等更贴合玩法的场景，提升观看意愿与 eCPM。
- **插屏**：P2-2 改为更克制的时机（关卡间隙、返回首页），避免结算即弹引发差评。
- **去广告会员 / 高级皮肤**：引入小程序虚拟支付（需合规）做"会员去广告 + 专属皮肤 + 高级报告"，开辟非广告收入，也照顾反感广告的用户。
- **当前状态**：广告单元为占位符 = 零收入，需优先替换并跑通真实填充。

### 5.5 口碑与合规
- 训练报告可分享卡片自带品牌曝光。
- 内容安全已三重防线，合规基础好；隐私政策/用户协议齐全，保持。

---

## 六、优化路线图（优先级排期）

| 阶段 | 目标 | 关键事项 |
|------|------|---------|
| **第 1 周（发布前必做）** | 修阻断、通闭环 | P0-1 难度档收敛；P0-2 成就可达；P1-1 填真实广告 ID + 优雅降级；P1-2 游客模式；P1-3 打卡/每日加成 |
| **第 2–4 周** | 体验打磨 | P2 全量修复；首页信息架构重排；结算页分享/可视化；主题+皮肤统一入口；Onboarding |
| **第 1–2 月** | 增长引擎 | 微信分享/挑战好友/群排行；订阅消息召回；训练报告周报 |
| **第 2–3 月** | 商业化与深度 | 去广告会员/高级皮肤；激励视频场景扩展；赛季/限时活动；难度自适应与个性化推荐 |

---

## 七、附录：问题速查表（文件:行号）

| 严重度 | 问题 | 位置 |
|--------|------|------|
| P0 | 每日/训练 scan·sort 引用非平方难度档，网格崩坏 | daily.js:24,28；training.js:11,20,30,41；scan-scene.js:145,165；sort-scene.js:124,146 |
| P0 | 9 个成就引用未写入字段，永久不可解锁 | achievements.js:17,18,22,23,24,25,26,27,32 |
| P1 | 广告占位符 → 看视频奖励全失效 | ads.js:7-9,20,69 |
| P1 | 强制登录门 + 未登录禁入排行/个人 | game.js:180-184；scene-manager.js:69；home-scene.js:93-97 |
| P1 | 打卡/每日积分加成未实现（文档不符） | scene.js:75-91,524-547；README:173-179 |
| P2 | scan 结算缺 gameState 守卫 | scan-scene.js:76,103,109 |
| P2 | 插屏结算即弹 | scene.js:542 |
| P2 | 记忆游戏返回无确认 | scene.js:489；memory-scene.js |
| P2 | 评分阈值散落各场景 | stroop:117；scan:116；sort:99 |
| P2 | 首页统计硬编码档位 | home-scene.js:677-678 |
| P2 | 皮肤命名 README/代码不一致 | README:198-207；skins.js:6-108 |
| P2 | react 假目标命中/视觉不符 | react-scene.js:154,102 |
| P2 | 清除数据未复位 unlockedSkins | profile-scene.js:161-168 |
| 增长缺口 | 无分享/转发/挑战好友/群排行 | 全局缺失 |
| 增长缺口 | 无订阅消息召回 | 全局缺失 |

---

*整理基于代码静态走查与产品/市场视角的体验推演；P0/P1 建议在上线推广前完成验证。*
