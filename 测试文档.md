# 专注风暴 - 全面测试文档

## 一、测试概述

本测试文档基于对项目全部代码的系统性审查，覆盖场景管理、游戏逻辑、UI 组件、数据同步、云函数和服务端共 6 大模块。共发现 **73 个问题**，按严重程度分类：

| 严重度 | 数量 | 说明 |
|--------|------|------|
| P0 严重 | 12 | 影响核心功能、数据安全或正确性 |
| P1 中等 | 18 | 影响用户体验或数据一致性 |
| P2 轻微 | 15 | 代码质量、健壮性或维护性问题 |
| P3 潜在 | 28 | 当前不触发但未来可能暴露的风险 |

---

## 二、P0 严重问题（必须修复）

### 2.1 游戏逻辑

#### TC-001: 斯特鲁普游戏重玩时 total 未重置
- **文件**: `js/scenes/stroop-scene.js:59-63`
- **现象**: 第一局玩 15 题后 total=15，第二局重玩时 total 从 15 继续累加，正确率计算为 correct/30 而非 correct/15
- **复现步骤**: 斯特鲁普游戏 → 完成一局 → 点击重新挑战 → 完成第二局 → 观察评级
- **期望**: 每局独立计算正确率
- **修复**: `initGame()` 中添加 `this.total = 0`

#### TC-002: updateBestScore 的 higherBetter 列表不完整
- **文件**: `js/app.js:85`
- **现象**: memory、scan、stroop 三个游戏的最佳成绩保存逻辑反转，越玩越差
- **复现步骤**: 记忆游戏第一局 10/10 → 第二局 8/10 → 查看最佳成绩显示 8
- **期望**: 最佳成绩应保留 10
- **修复**: `const higherBetter = ['match', 'memory', 'scan', 'stroop']`

#### TC-003: 色彩消除游戏死锁与超时双重结算
- **文件**: `js/scenes/match-scene.js:128-133, 147-162`
- **现象**: 死锁计时器(500ms)与主倒计时归零同时触发 `_gameFinished()`，积分加两次
- **复现步骤**: 色彩消除 → 制造死锁 → 在最后 0.5 秒内等待超时
- **期望**: 只结算一次
- **修复**: `_gameFinished()` 添加防重入保护 `if (this.gameState === 'finished') return`

#### TC-004: 扫描追踪游戏暂停恢复后计时器重置
- **文件**: `js/scenes/scan-scene.js:48-52, 71-78`
- **现象**: 暂停后恢复，倒计时从满时间重新开始
- **复现步骤**: 扫描追踪 → 开始游戏 → 暂停 → 恢复 → 观察倒计时
- **期望**: 从暂停时的剩余时间继续
- **修复**: `_startTimer()` 中改为 `this.startTime = Date.now() - (this.timeLimit - this.remainingTime) * 1000`

#### TC-005: 每日挑战从未标记为完成
- **文件**: 所有游戏场景的 `_gameFinished()`
- **现象**: 完成每日挑战后，首页卡片仍显示"未完成"
- **复现步骤**: 首页 → 点击每日挑战 → 完成游戏 → 返回首页
- **期望**: 每日挑战卡片显示"已完成"
- **修复**: `_gameFinished()` 中检查 `this._isDaily` 并更新 `userData.dailyChallenge.completed = true`

#### TC-006: 训练计划从未标记为完成
- **文件**: 所有游戏场景的 `_gameFinished()`
- **现象**: 训练计划的打卡功能完全失效
- **复现步骤**: 训练计划 → 完成一关 → 返回训练计划 → 观察打卡状态
- **期望**: 对应关卡标记为已完成
- **修复**: `_gameFinished()` 中检查 `this.params.training` 并更新 `userData.trainingPlan.completed`

### 2.2 触摸事件

#### TC-007: about-scene 返回按钮滚动后无法点击
- **文件**: `js/scenes/about-scene.js:63`
- **现象**: 向下滚动后，点击返回按钮的视觉位置无法命中
- **复现步骤**: 关于页面 → 向下滚动 → 点击返回按钮
- **期望**: 返回按钮始终可点击
- **修复**: `_hit(x, y + this.scrollY, this._backBtnRect)`

#### TC-008: training-scene 返回按钮滚动后无法点击
- **文件**: `js/scenes/training-scene.js:55`
- **现象**: 同 TC-007
- **修复**: `_hit(x, y + this.scrollY, this._backBtnRect)`

### 2.3 数据安全

#### TC-009: 服务端 /api/wxlogin 返回 session_key
- **文件**: `server/server.js:298`
- **现象**: session_key 被下发到客户端，可被用于伪造用户数据
- **期望**: 服务端存储 session_key，客户端只获取 session token
- **修复**: 移除返回值中的 session_key

#### TC-010: 排行榜 rankPoints 可被客户端篡改
- **文件**: `cloudfunctions/leaderboard/index.js:35`, `server/server.js:239`
- **现象**: 客户端可直接传入任意 rankPoints 值刷排行榜
- **期望**: 积分由服务端计算
- **修复**: 云函数和服务端拒绝客户端传入的 rankPoints，只接受增量

#### TC-011: 排行榜云函数 sync 分支竞态条件
- **文件**: `cloudfunctions/leaderboard/index.js:16-39`
- **现象**: 并发调用可能创建重复用户记录
- **修复**: 使用 upsert 语义或数据库唯一约束

#### TC-012: 服务端数据库读写无并发保护
- **文件**: `server/server.js:135-144`
- **现象**: 并发请求可能导致数据丢失
- **修复**: 添加文件锁或使用数据库替代 JSON 文件

---

## 三、P1 中等问题（建议修复）

### 3.1 游戏逻辑

#### TC-013: react/match/dual 不保存最佳成绩
- **文件**: `react-scene.js:111`, `match-scene.js:147`, `dual-scene.js:134`
- **现象**: 这三个游戏的最好成绩不会被记录
- **修复**: 在 `_gameFinished()` 中添加 `app.updateBestScore()` 调用

#### TC-014: react/match/sort/dual 重玩无结算音效
- **文件**: 四个场景的 `initGame()`
- **现象**: 重玩游戏后结算时没有音效
- **原因**: `initGame()` 未调用 `resetGameState()`，`_finishSoundPlayed` 未重置
- **修复**: 在 `initGame()` 中调用 `this.resetGameState()`

#### TC-015: 记忆游戏 300ms 延迟无取消保护
- **文件**: `js/scenes/memory-scene.js:91-93`
- **现象**: 退出游戏后 300ms 内 `_checkAnswer()` 仍会执行
- **修复**: 保存 setTimeout 引用，在 `onExit()` 中 clearTimeout

#### TC-016: 连续打卡功能未实现
- **文件**: 全局
- **现象**: `daily.js` 导出了 `calculateStreak()` 但从未被调用，首页连续打卡天数永远为 0
- **修复**: 在游戏完成后调用 `calculateStreak()` 更新 streak

#### TC-017: 补签卡逻辑无法正确恢复打卡
- **文件**: `js/utils/daily.js:79-89`
- **现象**: 断签后使用补签卡只能恢复 streak 为 1
- **修复**: 补签时恢复到断签前的 streak 值

### 3.2 触摸事件

#### TC-018: _handleReadyTouch 未处理 scrollY
- **文件**: `js/base/scene.js:482-496`
- **现象**: 准备页按钮碰撞检测坐标系不一致
- **修复**: 添加 `const sy = y + this.scrollY`

#### TC-019: _handleOverlayTouch 未处理 scrollY
- **文件**: `js/base/scene.js:428-478`
- **现象**: overlay 弹窗的碰撞检测坐标系不一致
- **修复**: 统一添加 scrollY 偏移

#### TC-020: switchTab 对当前 Tab 重复调用 onHide
- **文件**: `js/base/scene-manager.js:62-97`
- **现象**: 从游戏场景返回 Tab 时，Tab 被连续调用 onHide + onShow
- **修复**: 跳过对已暴露 Tab 的重复 onHide 调用

### 3.3 UI 组件

#### TC-021: 规则弹窗不支持内容滚动
- **文件**: `js/base/scene.js:249-267`
- **现象**: 规则文本超出卡片高度时无法查看完整内容
- **修复**: 添加滚动状态和触摸事件处理

#### TC-022: 惯性滚动阻尼系数帧率相关
- **文件**: `js/base/scene.js:81-89`
- **现象**: 120fps 设备上滚动几乎立即停止，60fps 设备上滑行较远
- **修复**: `this.scrollVelocity *= Math.pow(0.92, dt * 60)`

#### TC-023: PulseAnimator/FloatAnimator 无法单独停止
- **文件**: `js/base/animation.js:83, 102`
- **现象**: 无限循环动画只能全部清除，无法单独移除
- **修复**: 添加 `stop()` 方法或将 `done` 设为可写

### 3.4 数据同步

#### TC-024: 排行榜数据泄露完整用户对象
- **文件**: `cloudfunctions/leaderboard/index.js:49`
- **现象**: rankList 包含 openid 等敏感字段
- **修复**: 只返回 nickname、avatarUrl、rank、rankPoints、position

#### TC-025: 同分用户排名不一致
- **文件**: `cloudfunctions/leaderboard/index.js:49, 56-58`
- **现象**: rankList 和 myRank 的排名计算逻辑不同
- **修复**: 统一排名计算方式

#### TC-026: checkLocal 空值返回 pass:true
- **文件**: `js/utils/sensitive-filter.js:25-27`
- **现象**: null/undefined/非字符串类型直接通过检测
- **修复**: 空值返回 `pass: false`

---

## 四、P2 轻微问题

#### TC-027: dual-scene "红/蓝"任务对永远判定正确
- **文件**: `js/scenes/dual-scene.js:14`
- **修复**: `check: (num, zone) => num % 2 === 0`

#### TC-028: stroop-scene 评分逻辑与 scoring.js 重复
- **文件**: `js/scenes/stroop-scene.js:114-124`
- **修复**: 使用 `scoring.js` 的 `getStroopRating()`

#### TC-029: react-scene 重玩时 _hitTargets 未清空
- **文件**: `js/scenes/react-scene.js:60-64`
- **修复**: `initGame()` 中添加 `this._hitTargets = []`

#### TC-030: training.js dayIndex 时区问题
- **文件**: `js/utils/training.js:56-62`
- **修复**: 使用本地时间解析日期字符串

#### TC-031: scene-manager pop() 错误恢复逻辑有缺陷
- **文件**: `js/base/scene-manager.js:48-58`
- **修复**: onExit 异常时不压回场景，而是清理栈

#### TC-032: 登录云函数无错误处理
- **文件**: `cloudfunctions/login/index.js`
- **修复**: 添加 try/catch

#### TC-033: 排行榜 rank 分支无 try/catch
- **文件**: `cloudfunctions/leaderboard/index.js:44-63`
- **修复**: 添加 try/catch 和错误返回

#### TC-034: 敏感词列表两份副本不同步
- **文件**: `cloudfunctions/cleanSensitiveData/index.js:7-101` vs `js/utils/sensitive-words.js`
- **修复**: 统一使用同一份敏感词列表

#### TC-035: cleanSensitiveData API 错误码未区分违规和服务异常
- **文件**: `cloudfunctions/cleanSensitiveData/index.js:121`
- **修复**: 区分 errCode 87014（频率限制）和其他错误码

#### TC-036: 服务端 parseBody 无大小限制
- **文件**: `server/server.js:147-156`
- **修复**: 添加请求体大小限制

#### TC-037: 服务端错误响应状态码不一致
- **文件**: `server/server.js:190, 489`
- **修复**: 使用正确的 HTTP 状态码

#### TC-038: 服务端 CORS 配置过于宽松
- **文件**: `server/server.js:166`
- **修复**: 限制为允许的域名

#### TC-039: 服务端 /api/sync 和 /api/login 缺少 openid 格式校验
- **文件**: `server/server.js:223, 180`
- **修复**: 调用 `isValidOpenid()` 校验

#### TC-040: tab-bar.js getTabAt 缺少边界检查
- **文件**: `js/base/ui/tab-bar.js:96-100`
- **修复**: `index = Math.max(0, Math.min(2, index))`

#### TC-041: 多场景重复定义 _hit 方法
- **文件**: training-scene.js:65, about-scene.js:78, rank-scene.js:121
- **修复**: 删除重复定义，使用基类方法

---

## 五、测试用例矩阵

### 5.1 游戏功能测试

| 用例 | 游戏 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| G-001 | 全部 | 完成一局 → 点重新挑战 → 完成第二局 | 每局独立评分，最佳成绩取最优 | TC-002, TC-001 |
| G-002 | 全部 | 开始游戏 → 暂停 → 恢复 | 计时器从暂停处继续 | TC-004 |
| G-003 | 全部 | 完成一局 → 听结算音效 → 重玩 → 完成 → 听结算音效 | 每次结算都有音效 | TC-014 |
| G-004 | 色彩消除 | 制造死锁 → 等待超时 | 只结算一次 | TC-003 |
| G-005 | 斯特鲁普 | 第一局 15 题 → 重玩 → 第二局 15 题 | 每局正确率独立计算 | TC-001 |
| G-006 | 记忆游戏 | 输入答案 → 确认 → 立即退出 | 不会执行 _checkAnswer | TC-015 |
| G-007 | 双线任务 | 玩"红/蓝"任务对 | 需要正确判断红蓝区域 | TC-027 |
| G-008 | 反应速度 | 观察最后 5 秒 | 有倒计时音效 | TC-014 |

### 5.2 导航和触摸测试

| 用例 | 场景 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| N-001 | 关于页面 | 向下滚动 → 点击返回按钮 | 正确返回 | TC-007 |
| N-002 | 训练计划 | 向下滚动 → 点击返回按钮 | 正确返回 | TC-008 |
| N-003 | 排行榜 | 向下滚动 → 点击登录按钮 | 正确触发登录 | TC-019 |
| N-004 | 全部游戏 | 结算页向下滚动 → 点击返回首页 | 正确返回首页 | 已修复 |
| N-005 | 全部游戏 | 结算页向下滚动 → 点击下一难度 | 进入下一难度 | TC-018 |
| N-006 | 首页 | 点击游戏卡片 → 返回 → 点击另一游戏 | 正确导航 | TC-020 |
| N-007 | 全部游戏 | 结算页 → 看广告双倍 → 点击返回首页 | 正确返回首页 | TC-019 |

### 5.3 数据同步测试

| 用例 | 模块 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| D-001 | 排行榜 | 完成游戏 → 查看排行榜 | 排名显示正确 | TC-010, TC-011 |
| D-002 | 排行榜 | 多次完成游戏 → 查看排行榜 | 积分正确累加 | TC-010 |
| D-003 | 最佳成绩 | 记忆游戏 10/10 → 8/10 → 查看 | 最佳显示 10 | TC-002 |
| D-004 | 最佳成绩 | 扫描追踪 找到8个 → 找到6个 → 查看 | 最佳显示 8 | TC-002 |
| D-005 | 每日挑战 | 完成每日挑战 → 返回首页 | 显示"已完成" | TC-005 |
| D-006 | 训练计划 | 完成训练关卡 → 返回训练计划 | 打卡标记完成 | TC-006 |
| D-007 | 连续打卡 | 连续两天完成游戏 | 打卡天数正确递增 | TC-016 |
| D-008 | 云端同步 | 断网 → 完成游戏 → 联网 | 数据自动同步 | TC-024 |

### 5.4 安全测试

| 用例 | 模块 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| S-001 | 登录 | 抓包查看 /api/wxlogin 响应 | 不包含 session_key | TC-009 |
| S-002 | 排行榜 | 篡改 rankPoints 发送请求 | 服务端拒绝或忽略 | TC-010 |
| S-003 | 排行榜 | 传入非法 openid | 服务端返回 400 | TC-039 |
| S-004 | 内容安全 | 传入敏感词昵称 | 被拦截 | TC-026 |
| S-005 | 内容安全 | 传入 null/undefined | 被拦截 | TC-026 |
| S-006 | 排行榜 | 查看排行榜 API 响应 | 不包含 openid | TC-024 |

### 5.5 UI 组件测试

| 用例 | 组件 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| U-001 | 规则弹窗 | 打开规则 → 尝试滚动 | 内容可滚动查看 | TC-021 |
| U-002 | Tab 栏 | 快速切换多个 Tab | 不出现异常 | TC-020 |
| U-003 | 进度条 | 观察 1% 进度 | 圆角视觉正常 | - |
| U-004 | Toast | 连续触发多条 toast | 正确显示和消失 | - |
| U-005 | 引导弹窗 | 首次进入游戏 | 引导正确显示 | - |
| U-006 | 滚动 | 120fps 设备快速滑动 | 滚动距离与 60fps 一致 | TC-022 |

---

## 六、云函数和服务端测试

### 6.1 云函数测试

| 用例 | 函数 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| C-001 | login | 正常调用 | 返回 openid | TC-032 |
| C-002 | login | 异常环境调用 | 返回错误信息而非崩溃 | TC-032 |
| C-003 | leaderboard sync | 并发调用 | 不创建重复记录 | TC-011 |
| C-004 | leaderboard sync | 传入负数 rankPoints | 被拒绝或忽略 | TC-010 |
| C-005 | leaderboard rank | 正常调用 | 返回排行数据 | TC-033 |
| C-006 | leaderboard rank | 数据库异常 | 返回友好错误 | TC-033 |
| C-007 | contentCheck | 传入超长文本 | 正确处理 | - |
| C-008 | cleanSensitiveData | API 频率限制 | 不误清合法内容 | TC-035 |

### 6.2 服务端 API 测试

| 用例 | 接口 | 测试步骤 | 期望结果 | 关联问题 |
|------|------|----------|----------|----------|
| A-001 | /api/wxlogin | 正常登录 | 返回 openid，无 session_key | TC-009 |
| A-002 | /api/sync | 传入非法 openid | 返回 400 | TC-039 |
| A-003 | /api/sync | 并发请求 | 数据不丢失 | TC-012 |
| A-004 | /api/login | 传入 __proto__ | 返回 400 | TC-039 |
| A-005 | /api/rank | 正常请求 | 返回排行榜 | TC-038 |
| A-006 | /api/check-content | 传入敏感词 | 返回拦截 | TC-026 |
| A-007 | /api/upload-avatar | 传入非法文件名 | 返回 400 | - |
| A-008 | /api/check-image | 传入超大 base64 | 返回 413 | TC-036 |

---

## 七、回归测试清单

每次代码变更后需验证的核心功能：

- [ ] 完成任意一局游戏，积分正确累加
- [ ] 最佳成绩取最优值（不是最后一次的值）
- [ ] 排行榜显示自己的排名
- [ ] 暂停/恢复后计时器正确继续
- [ ] 重玩游戏时结算音效正常播放
- [ ] 返回首页按钮始终可点击
- [ ] 每日挑战完成后标记为已完成
- [ ] 训练计划完成后打卡标记
- [ ] 无 insertTextView 错误弹出
- [ ] 云端同步失败有 console.warn 输出

---

## 八、测试优先级建议

### 第一批（阻塞发布）
1. TC-002: higherBetter 列表修复
2. TC-001: stroop total 重置
3. TC-003: match 双重结算防重入
4. TC-004: scan 暂停恢复计时器
5. TC-005/006: 每日挑战和训练计划完成标记
6. TC-009: session_key 安全漏洞

### 第二批（影响体验）
7. TC-007/008: 返回按钮 scrollY 修复
8. TC-013: 三个游戏添加 updateBestScore
9. TC-014: 四个游戏添加 resetGameState
10. TC-016: 连续打卡功能实现
11. TC-018/019: 触摸坐标统一处理

### 第三批（质量提升）
12. TC-010/011/012: 数据安全加固
13. TC-021: 规则弹窗滚动支持
14. TC-022: 帧率无关滚动
15. TC-024/025: 排行榜数据安全
